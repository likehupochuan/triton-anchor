# T2.1 算子编译诊断 CLI 实现与测试结果

## 1. 实现概述

已完成首版 `triton-anchor-diagnose` 诊断 CLI，实现目标是将编译失败定位到具体 pass。

当前范围：

- 支持 Python/Triton 前端生成 TTIR 前失败的 pre-pass 诊断，阶段名为 `python-frontend`。
- 支持输入 `.ttir/.mlir` 在 MLIR parse 阶段失败的 pre-pass 诊断，阶段名为 `input-parse`。
- 支持 TTIR pipeline 逐 pass 诊断。
- 支持 triton-linalg adapter pipeline 逐 pass 诊断。
- 支持 Sophgo PPLIR pipeline 逐 pass 诊断，pipeline 名称为 `sophgo-pplir`。
- 支持 `ppl-compile`、CMake / `.so` 生成、runtime launch、结果/精度错误等非 pass 阶段的结构化诊断。
- 在每个 pass 执行前保存 IR 快照。
- 成功 pass 可保存 after IR 快照。
- 首个 pass 失败时停止，并报告失败 pass 名称、序号、错误信息和 before IR 路径。
- 尝试从 MLIR diagnostic / exception / 底层 stderr 中提取 MLIR location 与 op 名称。
- 失败时写入独立 `.diagnostic.txt`，保存错误、捕获到的底层诊断、location/op 和 traceback。
- 输出 `summary.json`，方便后续自动化分析。
- 提供 `python -m triton_anchor.diagnose` 和安装后的 `triton-anchor-diagnose` 命令入口。
- 支持 `--python MODULE:FUNCTION` 模式，在 CLI 运行时直接从 Python `@triton.jit` kernel 生成内存 TTIR 后诊断，不要求预先 dump `.ttir` 文件。
- 如果 `--python` 模式还没生成 TTIR 就失败，会写入 `python-frontend.diagnostic.txt` 和 `summary.json`。
- 如果文件输入在 `ir.parse_mlir_module()` 阶段失败，会写入 `input-parse.diagnostic.txt` 和 `summary.json`。
- 支持 Sophgo JIT 编译失败时按环境变量自动触发诊断；覆盖 TTIR、`triton-linalg`、PPLIR、`ppl-compile`、CMake / `.so`、runtime launch 和测试结果/精度 mismatch。

## 2. 修改文件

### 2.1 新增文件及作用

| 文件或目录 | 作用 |
| --- | --- |
| `python/triton_anchor/diagnostics.py` | 诊断核心实现。新增 `PassDiagnostic`、pass descriptor、运行记录、summary 数据结构；负责逐 pass 执行 TTIR / `triton-linalg` / `sophgo-pplir` pipeline，保存 IR 快照，捕获失败诊断并提取 MLIR op/location；新增 `StageDiagnostic` 覆盖 `ppl-compile`、CMake / `.so`、runtime、result mismatch 这类非 pass 阶段；新增 `extract_mlir_location()` 供 pre-pass 和外部阶段诊断复用同一套 location/op 解析逻辑。 |
| `python/triton_anchor/diagnose.py` | CLI 入口实现。负责参数解析、输入 IR 加载、`--python MODULE:FUNCTION` 运行时生成 TTIR、调用 `PassDiagnostic` 并打印诊断结果；新增 `InputDiagnosticError` / `InputDiagnosticResult`，覆盖 `python-frontend` 和 `input-parse` 两类进入 pass 前的失败。 |
| `python/triton_anchor/tests/test_diagnostics.py` | 单元测试。覆盖首个失败 pass 定位、MLIR op/location 提取、成功/失败 summary、`triton-linalg` 诊断入口、CLI `--python` 模式、`--help`、非法 MLIR parse 诊断、Python frontend 诊断。 |
| `ops-diagnose-cli/docs/T2.1-implementation-and-test-results.md` | 实现与测试结果文档。记录最终实现内容、修改文件、验证命令、测试结果和故障样例输出。 |
| `docs/diagnostics.md` | 面向使用者的正式说明文档。说明当前能诊断哪些阶段、CLI 怎么用、JIT 自动诊断怎么开、输出文件怎么看、当前边界是什么。 |
| `ops-diagnose-cli/test/anchor_diag_bad_kernels.py` | 故障样例 kernel。使用 `tl.inline_asm_elementwise` 构造真实 lowering 失败，用来验证能定位到 `triton_linalg.triton_to_linalg` pass 和 `tt.elementwise_inline_asm` op；同时提供 `__main__` 启动逻辑，可配合 `TRITON_ANCHOR_DIAGNOSE_ON_ERROR=1` 直接 `python anchor_diag_bad_kernels.py` 生成内存 TTIR 并诊断 TTIR / `triton-linalg` pipeline。 |
| `ops-diagnose-cli/test/anchor_diag_bad_frontend_kernel.py` | frontend 失败样例 kernel。使用不存在的 `tl.this_builtin_does_not_exist(x)` 构造 TTIR 生成前错误；可配合 `TRITON_ANCHOR_DIAGNOSE_ON_ERROR=1` 直接运行，验证 `python-frontend.diagnostic.txt` 和 pre-pass `summary.json` 输出。 |
| `ops-diagnose-cli/test/anchor_diag_bad_pplir_kernel.py` | PPLIR 失败样例 kernel。构造 `tensor<4x16xf32> -> tensor<4x8x16xf32>` 的 2D 到 3D broadcast，TTIR 和 `triton-linalg` 均可通过，最终在 Sophgo `sophgo.linalg_to_ppl` pass 的 C 维 broadcast 限制处失败；用于验证 PPLIR pass/op/location 定位。 |
| `ops-diagnose-cli/test/bad-inline-ttir-diagnose.log` | 故障样例第一阶段验证日志。记录 bad inline asm kernel 生成并通过 TTIR pipeline 的输出；属于运行验证产物，不是功能源码。 |
| `ops-diagnose-cli/test/bad-inline-ttir-diagnose/` | TTIR pipeline 诊断产物目录。保存该样例每个 TTIR pass 的 before/after IR 和 `summary.json`；属于运行验证产物，不建议随源码提交。 |
| `ops-diagnose-cli/test/bad-inline-linalg-diagnose.log` | 故障样例第二阶段验证日志。记录 `triton-linalg` pipeline 失败时 CLI 输出的 pass/op/location；属于运行验证产物，不是功能源码。 |
| `ops-diagnose-cli/test/bad-inline-linalg-diagnose/` | `triton-linalg` 失败诊断产物目录。保存失败前 IR、`07-triton_linalg.triton_to_linalg.diagnostic.txt` 和 `summary.json`；属于运行验证产物，不建议随源码提交。 |

### 2.2 修改文件及作用

| 文件 | 修改内容与作用 |
| --- | --- |
| `python/triton_anchor/__init__.py` | 导出诊断相关能力，使 `triton_anchor` 包对外暴露新增诊断模块。 |
| `setup.py` | 注册 `console_scripts`，安装后生成 `triton-anchor-diagnose` 命令；同时保留原有 adapter entry point。 |
| `pyproject.toml` | 同步注册 `[project.scripts]`，避免不同安装方式下 CLI 命令未生成。 |
| `/workspace/triton-sophgo-backend/triton_sophgo/compiler.py` | 在 Sophgo JIT 后端 `_make_ttir()`、`_make_linalg()`、`_make_pplir()`、`ppl-compile`、CMake / `.so` 生成异常路径中接入可选诊断 hook。设置 `TRITON_ANCHOR_DIAGNOSE_ON_ERROR=1` 后，任意通过 `fn[grid](...)` 进入 Sophgo 后端的新旧算子，如果在这些阶段失败，会自动输出失败 pass/stage、op/location、命令、return code、诊断产物路径。默认不开启，不改变正常编译行为。 |
| `/workspace/triton-sophgo-backend/triton_sophgo/driver.py` | 在 Sophgo runtime launcher 调用 `torch_tpu.CallCppDynLib(...)` 的异常路径中接入 `runtime-launch` 诊断，记录 `.so`、entry、grid、参数 shape/dtype/device/data_ptr 和 traceback。 |
| `/workspace/triton-sophgo-backend/tests/conftest.py` | 在测试 `assert_close()` 数值比较失败时接入 `result-mismatch` 诊断，记录 `rtol/atol`、最大绝对/相对误差、不匹配数量、actual/expected tensor 摘要和 mismatch 样本。 |

### 2.3 未修改内容说明

- 本次 T2.1 诊断能力没有修改 C++ 文件。实现复用已有 `triton._C.libtriton` Python 绑定、现有 pass 注册函数，以及 C++/MLIR pass 输出到 stderr 的 diagnostic。
- 当前自动诊断 hook 覆盖 TTIR、`triton-linalg`、PPLIR、`ppl-compile`、CMake / `.so` 生成、runtime launch 和测试结果/精度 mismatch。
## 3. 核心实现

### 3.1 `PassDiagnostic`

实现位置：

```text
python/triton_anchor/diagnostics.py
```

新增主要类型：

- `PassDescriptor`
- `PassRunRecord`
- `PassDiagnosticResult`
- `PassDiagnostic`

核心行为：

1. 构造 TTIR pass descriptor 列表。
2. 每次创建新的 MLIR `PassManager`。
3. 每次只向 `PassManager` 添加一个 pass。
4. 执行前写入 before IR。
5. pass 成功后可写入 after IR。
6. pass 失败时捕获 Python exception 和底层 stderr diagnostic。
7. 从诊断文本中提取 MLIR file/line/column/op，尽量映射回 before IR 行。
8. pass 失败时记录异常、traceback、失败 pass 名称、序号、location/op 和诊断详情文件。
9. 写入 `summary.json`。

首版 TTIR pass 列表与 `pipeline.py` 中的 mandatory pipeline 对齐：

```text
common.inliner
ttir.combine
common.canonicalizer
ttir.reorder_broadcast
common.cse
common.licm
common.symbol_dce
```

同时保留了基于 `HWCapability` 追加条件 pass 的能力：

- `ttir.rewrite_tensor_pointer`
- `ttir.loop_unroll`
- `ttir.expression_restructing`

triton-linalg adapter pipeline 已接入：

```text
triton_linalg.wrap_func_body_with_single_block.pre
common.inliner
common.canonicalizer.pre
triton_linalg.canonicalize_triton
triton_linalg.pointer_strength_reduction
common.canonicalizer.after_pointer_strength_reduction
triton_linalg.triton_to_linalg
triton_linalg.extract_like_move_backward
common.canonicalizer.post_conversion
triton_linalg.arith_to_linalg
triton_linalg.math_to_linalg
common.cse
common.licm
triton_linalg.wrap_func_body_with_single_block.final
```

Sophgo PPLIR pipeline 已接入：

```text
sophgo.triton_to_ppl
sophgo.linalg_to_ppl
common.cse.pplir
common.canonicalizer.pplir
```

`sophgo.linalg_to_ppl` 后的 PPLIR cleanup pass 当前关闭 verifier 逐 pass 诊断，避免中间 PPLIR 在局部转换后尚未完全合法化时被 verifier 提前截断。

### 3.2 诊断原理与源码对应

#### 3.2.1 pass 定位原理

原正常编译流程是将整条 pipeline 的多个 pass 一次性加入同一个 MLIR `PassManager`，然后执行一次 `pm.run(mod)`。这种方式失败时只能知道整个 pipeline 失败，不能稳定判断哪个 pass 首先失败。

诊断模式改为逐 pass 执行：

1. 将 pipeline 拆成 `PassDescriptor(name, add_to_pass_manager)` 列表。
2. 每轮只创建一个新的 `PassManager`。
3. 每轮只加入当前一个 pass。
4. 执行当前 pass 前保存 before IR。
5. 当前 pass 成功后继续下一个 pass。
6. 当前 pass 抛异常时立即停止，并记录当前 pass 名称和序号。

核心源码：

```text
python/triton_anchor/diagnostics.py
```

对应函数和逻辑：

```text
PassDiagnostic._diagnose_pipeline()
```

关键代码位置：

```text
for index, descriptor in enumerate(descriptors, start=1):
    before_ir_text = str(mod)
    before_path = self._write_ir(index, descriptor.name, "before", mod)
    pm = ir.pass_manager(mod.context)
    descriptor.add_to_pass_manager(pm)

    try:
        diagnostic_text = self._run_pass(pm, mod)
    except Exception:
        result.failed_pass = descriptor.name
        result.failed_index = index
        ...
        return result
```

因此，`descriptor.name` 就是失败 pass 名称，`index` 就是失败 pass 序号，`before_path` 是失败前 IR 快照。

TTIR pass descriptor 构造位置：

```text
python/triton_anchor/diagnostics.py::build_ttir_pass_descriptors()
```

`triton-linalg` pass descriptor 构造位置：

```text
python/triton_anchor/diagnostics.py::build_triton_linalg_pass_descriptors()
```

#### 3.2.2 op/location 定位原理

具体 op/location 不是通过修改 C++ pass 获得的，而是 best-effort 解析底层 MLIR/C++ diagnostic：

1. 执行单个 pass 时，临时捕获进程 `stderr`。
2. MLIR/C++ pass 报错通常会输出类似：

   ```text
   loc("kernel.py":14:8): error: failed to legalize operation 'tt.elementwise_inline_asm'
   ```

3. Python 侧从 exception 文本和 captured stderr 中解析：
   - `loc("file":line:column)`
   - `failed to legalize operation 'tt.xxx'`
4. 如果报错里没有直接给出 op，则尝试用 location 回查 before IR 中对应行，再从 IR 行提取 op。

捕获 stderr 的源码：

```text
python/triton_anchor/diagnostics.py::_run_with_stderr_capture()
```

op/location 解析入口：

```text
python/triton_anchor/diagnostics.py::_extract_mlir_location()
```

location 正则匹配：

```text
python/triton_anchor/diagnostics.py::_find_location_match()
```

op 正则匹配：

```text
python/triton_anchor/diagnostics.py::_extract_operation_from_text()
```

用 location 回查 before IR：

```text
python/triton_anchor/diagnostics.py::_find_ir_line_for_location()
python/triton_anchor/diagnostics.py::_extract_operation_from_ir_line()
```

这个定位是 best-effort：如果底层 pass 只输出 `PassManager::run failed`，没有 location 或 op，工具仍能稳定定位失败 pass 和 before IR，但不一定能定位到具体 op。

#### 3.2.3 CLI 与 JIT hook 对应源码

CLI 入口：

```text
python/triton_anchor/diagnose.py::main()
```

CLI 根据参数调用：

```text
PassDiagnostic.diagnose_ttir()
PassDiagnostic.diagnose_triton_linalg()
```

`--python MODULE:FUNCTION` 运行时生成 TTIR 的源码：

```text
python/triton_anchor/diagnose.py::_make_ttir_from_python()
```

Sophgo JIT 自动诊断 hook 源码：

```text
/workspace/triton-sophgo-backend/triton_sophgo/compiler.py
```

关键函数：

```text
_is_anchor_diagnose_on_error_enabled()
_run_anchor_diagnose_on_error()
_make_ttir()
_make_linalg()
```

触发逻辑：

- `_make_ttir()` 中 `pm.run(mod)` 抛异常时，如果 `TRITON_ANCHOR_DIAGNOSE_ON_ERROR=1`，调用 `PassDiagnostic.diagnose_ttir()`。
- `_make_linalg()` 中 `pm.run(mod)` 抛异常时，如果 `TRITON_ANCHOR_DIAGNOSE_ON_ERROR=1`，调用 `PassDiagnostic.diagnose_triton_linalg()`。

### 3.3 MLIR op/location 定位

失败时会综合以下来源做 best-effort 解析：

- pass 抛出的 exception 文本。
- `pm.run()` 期间底层 C++/MLIR 写到 stderr 的 diagnostic。
- 当前 pass 执行前的 before IR 快照。

当前可识别的 location 形式包括：

```text
loc("file":line:column)
file.mlir:line:column
file.ttir:line:column
<stdin>:line:column
line:column
```

当前可识别的 op 形式包括：

```text
failed to legalize operation 'tt.load'
operation 'tt.store'
"tt.load"(...)
tt.store ...
C dimension affected, case not supported yet. -> linalg.broadcast
unsupported transpose / cw transpose -> linalg.transpose
```

如果 diagnostic 只给出 `#locN = loc("file":line:column)` 形式，诊断器会继续回查 before IR 中引用 `loc(#locN)` 的具体 operation 行；例如 PPLIR broadcast 失败会落到 `linalg.broadcast` 那一行，而不是只停在 `#locN` 定义行。

定位结果会写入：

- CLI 输出。
- `summary.json`。
- `NN-pass-name.diagnostic.txt`。

### 3.4 pre-pass 诊断

新增能力覆盖“还没进入 pass”的两类失败：

```text
python/triton_anchor/diagnose.py
```

核心类型：

- `InputDiagnosticError`
- `InputDiagnosticResult`

#### 3.4.1 Python/Triton 前端失败

触发路径：

```text
python_triton kernel -> triton.compiler.ASTSource -> src.make_ir() -> TTIR
```

如果在导入 Python kernel、解析签名/constexpr、或 `src.make_ir()` 生成 TTIR 之前失败，CLI 不再只打印一行 `failed to prepare input module`，而是写入结构化诊断：

```text
stage: python-frontend
diagnostic detail: python-frontend.diagnostic.txt
summary: summary.json
```

实现位置：

```text
python/triton_anchor/diagnose.py::_make_ttir_from_python()
python/triton_anchor/diagnose.py::_write_input_diagnostic()
python/triton_anchor/diagnose.py::_print_input_diagnostic()
```

实现方式：

1. `triton` / `libtriton` 导入成功后，执行 Python kernel 加载和 TTIR 生成。
2. 对 `src.make_ir()` 增加 stderr 捕获，保留底层 frontend/MLIR diagnostic。
3. 失败时抛出 `InputDiagnosticError(stage="python-frontend")`。
4. CLI 捕获该异常后写 `python-frontend.diagnostic.txt` 和 `summary.json`。
5. 复用 `diagnostics.py::extract_mlir_location()` 从错误文本中 best-effort 提取 `file:line:column` 和 op。

该阶段还没有生成合法 TTIR module，所以不会有 failed pass，也不会有 `*.before.mlir`。诊断结果定位的是失败阶段和 frontend diagnostic。

#### 3.4.2 MLIR parse 失败

触发路径：

```text
input .ttir/.mlir -> ir.parse_mlir_module()
```

如果输入文件自身非法，`ir.parse_mlir_module()` 阶段失败，CLI 会写入：

```text
stage: input-parse
diagnostic detail: input-parse.diagnostic.txt
summary: summary.json
```

实现位置：

```text
python/triton_anchor/diagnose.py::_load_mlir_module()
python/triton_anchor/diagnose.py::_capture_stderr()
python/triton_anchor/diagnose.py::_write_input_diagnostic()
```

实现方式：

1. 文件存在性检查仍然保留，文件不存在直接返回参数错误。
2. `ir.parse_mlir_module()` 通过 `_capture_stderr()` 执行，捕获底层 MLIR parse diagnostic。
3. parse 失败时抛出 `InputDiagnosticError(stage="input-parse")`。
4. CLI 写 `input-parse.diagnostic.txt` 和 `summary.json`。
5. 如果 diagnostic 中包含 `file:line:column`，会读取原始输入文件，尽量回查对应 IR 行和 op。

该阶段同样还没有进入 pass，因此不会报告 failed pass。诊断目标是确认失败发生在输入 parse，而不是后续 TTIR / `triton-linalg` pass。

### 3.5 CLI 入口

实现位置：

```text
python/triton_anchor/diagnose.py
```

支持调用方式：

```bash
python -m triton_anchor.diagnose input.ttir --pipeline ttir
python -m triton_anchor.diagnose input.ttir --pipeline triton-linalg
python -m triton_anchor.diagnose input.mlir --pipeline sophgo-pplir
python -m triton_anchor.diagnose --python tests.test_smoke:_smoke_add_kernel --signature '*fp32,*fp32,*fp32,i32' --constant BLOCK=256
triton-anchor-diagnose input.ttir --pipeline ttir
triton-anchor-diagnose input.ttir --pipeline triton-linalg
triton-anchor-diagnose input.mlir --pipeline sophgo-pplir
triton-anchor-diagnose --python tests.test_smoke:_smoke_add_kernel --signature '*fp32,*fp32,*fp32,i32' --constant BLOCK=256
```

CLI 参数：

```text
input
--python MODULE:FUNCTION
--signature SIGNATURE
--constant NAME_OR_INDEX=VALUE
--pipeline {ttir,triton-linalg,sophgo-pplir}
--output-dir OUTPUT_DIR
--no-debug
--no-success-snapshots
--no-summary-json
--quiet
```

退出码约定：

- `0`：诊断 pipeline 成功。
- `1`：诊断过程中 pass 或运行逻辑失败。
- `2`：输入文件、参数或环境错误。

`--python` 模式约束：

- 只支持 `--pipeline ttir`。
- `--signature` 必填。
- `--constant` 可重复，key 可以是参数名或参数下标。
- 不需要预先存在 `.ttir` 文件；CLI 内部生成 TTIR module 后直接调用 `PassDiagnostic`。

失败输出会额外包含：

```text
diagnostic detail: /workspace/triton-anchor/diagnose-output/example/NN-pass-name.diagnostic.txt
location: file:line:column
operation: tt.load
ir line: N: %0 = "tt.load"(...)
```

### 3.6 `StageDiagnostic`

实现位置：

```text
python/triton_anchor/diagnostics.py
```

新增主要类型：

- `StageDiagnosticResult`
- `StageDiagnostic`

用途：

- 覆盖没有 MLIR pass manager 的阶段，例如 `ppl-compile`、CMake、`.so` 查找、runtime launch、result mismatch。
- 统一写入 `<stage>.diagnostic.txt` 和 `summary.json`。
- 记录 command、return code、输入 IR、工作目录、外部工具输出、traceback、附加 artifacts/extra。
- 复用 `extract_mlir_location()` 从外部工具输出中 best-effort 提取 MLIR location/op。

典型输出：

```text
ppl-compile.diagnostic.txt
cmake.diagnostic.txt
so-generation.diagnostic.txt
runtime-launch.diagnostic.txt
result-mismatch.diagnostic.txt
summary.json
```

### 3.7 entry point 注册

`setup.py` 中新增：

```python
"console_scripts": [
    "triton-anchor-diagnose = triton_anchor.diagnose:main",
],
```

`pyproject.toml` 中新增：

```toml
[project.scripts]
triton-anchor-diagnose = "triton_anchor.diagnose:main"
```

由于仓库同时存在 `setup.py` 和 PEP 621 `[project]` 元数据，两个位置都注册可以避免安装方式差异导致命令没有生成。

### 3.8 Sophgo JIT 失败自动诊断

实现位置：

```text
/workspace/triton-sophgo-backend/triton_sophgo/compiler.py
```

新增环境变量开关：

```bash
TRITON_ANCHOR_DIAGNOSE_ON_ERROR=1
```

默认不开启，不影响正常 JIT 编译性能和输出。开启后，任意通过 Sophgo 后端 `fn[grid](...)` 触发编译的算子，如果在 `_make_ttir()`、`_make_linalg()` 或 `_make_pplir()` 阶段失败，会自动调用 `PassDiagnostic` 复跑对应 pipeline；如果在 `ppl-compile`、CMake / `.so`、runtime launch 或测试结果比较阶段失败，会调用 `StageDiagnostic` 写入对应 stage 诊断。

```text
[AnchorDiagnose] pipeline ...
[AnchorDiagnose] before IR: ...
[AnchorDiagnose] diagnostic detail: ...
[AnchorDiagnose] location: file:line:column
[AnchorDiagnose] operation: ...
[AnchorDiagnose] summary: ...
```

诊断输出目录规则：

1. 如果设置 `TRITON_ANCHOR_DIAGNOSE_DIR`，写入该目录。
2. 否则如果设置 `TRITON_DUMP_DIR`，写入对应 kernel dump 目录下的 `anchor-diagnose/<pipeline>/`。
3. 否则写入当前工作目录下的 `triton-anchor-diagnose/<kernel>/<pipeline>/`。

该能力不是只针对 `tests/test_jit.py` 中已有四个算子；只要新增算子仍走 Sophgo JIT 后端，失败时都会进入同一套诊断逻辑。结果/精度错误需要测试或业务代码提供 expected output；当前已在 Sophgo 测试 `assert_close()` 中接入。

## 4. 测试覆盖

测试文件：

```text
python/triton_anchor/tests/test_diagnostics.py
```

测试内容：

1. `PassDiagnostic` 可以定位首个失败 pass。
2. 失败时写入 before IR 和 `summary.json`。
3. 成功时写入 after IR 和 `summary.json`。
4. 失败时可以提取 MLIR location 和 operation。
5. PPLIR 的 `C dimension affected` diagnostic 可以映射到 `linalg.broadcast`，并通过 `#locN` 回查到 before IR 中的具体 op 行。
6. `triton-linalg` pipeline 可以通过同一套诊断机制定位失败 pass。
7. Sophgo PPLIR pipeline 可以通过同一套诊断机制定位失败 pass。
8. `StageDiagnostic` 可以写入外部阶段失败的 diagnostic 和 summary，并从外部工具输出中提取 location/op。
9. CLI `--python` 模式可以诊断内存中生成的 TTIR module。
10. CLI 可以对非法输入 IR 的 MLIR parse 失败写入 `input-parse.diagnostic.txt` 和 `summary.json`。
11. CLI 可以对 Python/Triton frontend 生成 TTIR 前失败写入 `python-frontend.diagnostic.txt` 和 `summary.json`。
12. CLI `--help` 可以在没有真实 `triton._C.libtriton` 的环境中加载。
13. Sophgo JIT on-error hook 做了源码编译检查，确认 `TRITON_ANCHOR_DIAGNOSE_ON_ERROR` 接入代码可加载。

测试采用 fake `PassManager` 和 fake `triton._C.libtriton.ir`，因此不依赖完整 C++/MLIR 构建产物。

## 5. 验证结果

### 5.1 验证环境

验证命令应在已完成 Triton Anchor 构建安装、并能加载相关 Python 包的开发环境中执行。本次记录的环境信息如下：

```text
workdir: /workspace/triton-anchor
python: /opt/venv/bin/python
python version: Python 3.12.12
pytest version: pytest 9.1.1
```

注意：下面的源码路径是相对于仓库根目录 `/workspace/triton-anchor` 的。因此执行验证命令前需要先进入仓库目录：

```bash
cd /workspace/triton-anchor
source /opt/venv/bin/activate
```

环境确认命令：

```bash
cd /workspace/triton-anchor
pwd
/opt/venv/bin/python -V
/opt/venv/bin/python -m pytest --version
```

结果：

```text
/workspace/triton-anchor
Python 3.12.12
pytest 9.1.1
```

### 5.2 源码编译检查

执行命令：

```bash
cd /workspace/triton-anchor
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=python /opt/venv/bin/python - <<'PY'
from pathlib import Path
for path in [
    'python/triton_anchor/diagnostics.py',
    'python/triton_anchor/diagnose.py',
    'python/triton_anchor/tests/test_diagnostics.py',
    '/workspace/triton-sophgo-backend/triton_sophgo/compiler.py',
]:
    compile(Path(path).read_text(), path, 'exec')
print('compile ok')
PY
```

结果：

```text
compile ok
```

### 5.3 运行新增测试

该测试用于保证诊断核心逻辑、CLI pre-pass 诊断、PPLIR op/location 解析等关键路径在后续修改中不会回退。

执行命令：

```bash
cd /workspace/triton-anchor
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=python /opt/venv/bin/python -m pytest python/triton_anchor/tests/test_diagnostics.py -q
```

结果：

```text
...........                                                              [100%]
11 passed in 0.10s
```

### 5.4 CLI help 验证

执行命令：

```bash
cd /workspace/triton-anchor
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=python /opt/venv/bin/python -m triton_anchor.diagnose --help
```

结果：正常输出帮助信息。

```text
usage: triton-anchor-diagnose [-h] [--python MODULE:FUNCTION]
                              [--signature SIGNATURE]
                              [--constant NAME_OR_INDEX=VALUE]
                              [--pipeline {ttir,triton-linalg,sophgo-pplir}]
                              [--output-dir OUTPUT_DIR] [--no-debug]
                              [--no-success-snapshots] [--no-summary-json]
                              [--quiet]
                              [input]

Run Triton Anchor compiler pipelines one pass at a time and report the first
failing pass.

positional arguments:
  input                 Input MLIR/TTIR file to diagnose.

options:
  -h, --help            show this help message and exit
  --python MODULE:FUNCTION
                        Generate TTIR in memory from a Python @triton.jit
                        function and diagnose it without requiring a pre-
                        dumped IR file.
  --signature SIGNATURE
                        Kernel signature for --python mode. Use comma-
                        separated argument types, e.g.
                        '*fp32,*fp32,*fp32,i32'.
  --constant NAME_OR_INDEX=VALUE
                        Compile-time constant for --python mode. Can be
                        repeated. Example: --constant BLOCK=256 or --constant
                        4=256.
  --pipeline {ttir,triton-linalg,sophgo-pplir}
                        Compiler pipeline to diagnose. Default: ttir.
  --output-dir OUTPUT_DIR
                        Directory for IR snapshots and summary.json.
  --no-debug            Do not enable MLIR PassManager debug mode.
  --no-success-snapshots
                        Only save before snapshots, not after snapshots for
                        successful passes.
  --no-summary-json     Do not write summary.json.
  --quiet               Print only the final status line.
```

### 5.5 Python kernel 内存诊断验证

执行命令：

```bash
cd /workspace/triton-anchor
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=/workspace/triton-anchor/python:/workspace/triton-anchor \
python3 -m triton_anchor.diagnose \
  --python tests.test_smoke:_smoke_add_kernel \
  --signature '*fp32,*fp32,*fp32,i32' \
  --constant BLOCK=256 \
  --pipeline ttir \
  --output-dir /workspace/triton-anchor/diagnose-output/smoke-python
```

结果：

```text
OK: pipeline ttir completed, 7/7 passes executed.
diagnostic output: /workspace/triton-anchor/diagnose-output/smoke-python
summary: /workspace/triton-anchor/diagnose-output/smoke-python/summary.json
```

该验证没有预先提供 `.ttir` 文件。CLI 在运行时导入 `tests.test_smoke:_smoke_add_kernel`，生成内存 TTIR module 后直接诊断。输出目录仍包含诊断产物：

```text
01-common.inliner.before.mlir
01-common.inliner.after.mlir
...
07-common.symbol_dce.before.mlir
07-common.symbol_dce.after.mlir
summary.json
```

### 5.6 编译错误算子失败定位验证

为了验证诊断 CLI 在真实 pass 失败时是否能定位清楚，本次在仓库内新增了专门的测试目录和错误 Triton kernel 文件：

```text
ops-diagnose-cli/test/anchor_diag_bad_kernels.py
```

故意引入的问题是使用 `tl.inline_asm_elementwise`：

```python
@triton.jit
def bad_inline_asm_kernel(x_ptr, out_ptr, n, BLOCK: tl.constexpr):
    pid = tl.program_id(0)
    offs = pid * BLOCK + tl.arange(0, BLOCK)
    mask = offs < n
    x = tl.load(x_ptr + offs, mask=mask)
    y = tl.inline_asm_elementwise(
        "mov.u32 $0, $1;",
        "=r,r",
        [x.to(tl.int32)],
        dtype=tl.int32,
        is_pure=True,
        pack=1,
    )
    tl.store(out_ptr + offs, y.to(tl.float32), mask=mask)
```

这个改错点不是语法错误。它可以生成合法 TTIR，并且可以通过 TTIR 7 个 pass；失败发生在后续 `triton-linalg` adapter pipeline 中，适合验证 pass 级诊断。

第一步：从测试 Python kernel 生成并诊断 TTIR pipeline。诊断输出和命令日志都写入 `ops-diagnose-cli/test/`。

```bash
cd /workspace/triton-anchor
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=/workspace/triton-anchor/ops-diagnose-cli/test:/workspace/triton-anchor/python:/workspace/triton-anchor \
/opt/venv/bin/python -m triton_anchor.diagnose \
  --python anchor_diag_bad_kernels:bad_inline_asm_kernel \
  --signature '*fp32,*fp32,i32' \
  --constant BLOCK=256 \
  --pipeline ttir \
  --output-dir /workspace/triton-anchor/ops-diagnose-cli/test/bad-inline-ttir-diagnose \
  > /workspace/triton-anchor/ops-diagnose-cli/test/bad-inline-ttir-diagnose.log 2>&1
```

`ops-diagnose-cli/test/bad-inline-ttir-diagnose.log` 结果：

```text
Arch sg2260 specified by env var
OK: pipeline ttir completed, 7/7 passes executed.
diagnostic output: /workspace/triton-anchor/ops-diagnose-cli/test/bad-inline-ttir-diagnose
summary: /workspace/triton-anchor/ops-diagnose-cli/test/bad-inline-ttir-diagnose/summary.json
```

第二步：把 TTIR pipeline 的最终 IR 交给 `triton-linalg` adapter pipeline 诊断。

```bash
cd /workspace/triton-anchor
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=/workspace/triton-anchor/python:/workspace/triton-anchor \
/opt/venv/bin/python -m triton_anchor.diagnose \
  /workspace/triton-anchor/ops-diagnose-cli/test/bad-inline-ttir-diagnose/07-common.symbol_dce.after.mlir \
  --pipeline triton-linalg \
  --output-dir /workspace/triton-anchor/ops-diagnose-cli/test/bad-inline-linalg-diagnose \
  > /workspace/triton-anchor/ops-diagnose-cli/test/bad-inline-linalg-diagnose.log 2>&1
```

该命令预期返回 exit code `1`，因为它就是故意构造的 adapter pass 失败样例。

`ops-diagnose-cli/test/bad-inline-linalg-diagnose.log` 结果：

```text
Arch sg2260 specified by env var
FAILED: pipeline triton-linalg failed at pass 7/14: triton_linalg.triton_to_linalg
before IR: /workspace/triton-anchor/ops-diagnose-cli/test/bad-inline-linalg-diagnose/07-triton_linalg.triton_to_linalg.before.mlir
diagnostic detail: /workspace/triton-anchor/ops-diagnose-cli/test/bad-inline-linalg-diagnose/07-triton_linalg.triton_to_linalg.diagnostic.txt
location: /workspace/triton-anchor/ops-diagnose-cli/test/anchor_diag_bad_kernels.py:23:8
operation: tt.elementwise_inline_asm
ir line: 34: #loc10 = loc("/workspace/triton-anchor/ops-diagnose-cli/test/anchor_diag_bad_kernels.py":23:8)
error: PassManager::run failed
diagnostic output: /workspace/triton-anchor/ops-diagnose-cli/test/bad-inline-linalg-diagnose
summary: /workspace/triton-anchor/ops-diagnose-cli/test/bad-inline-linalg-diagnose/summary.json
```

诊断详情文件内容：

```text
pass: triton_linalg.triton_to_linalg
error: PassManager::run failed
location:
  raw: loc("/workspace/triton-anchor/ops-diagnose-cli/test/anchor_diag_bad_kernels.py":23:8)
  file: /workspace/triton-anchor/ops-diagnose-cli/test/anchor_diag_bad_kernels.py
  line: 23
  column: 8
  operation: tt.elementwise_inline_asm
  ir_line: 34
  ir_snippet: #loc10 = loc("/workspace/triton-anchor/ops-diagnose-cli/test/anchor_diag_bad_kernels.py":23:8)
captured diagnostics:
loc("/workspace/triton-anchor/ops-diagnose-cli/test/anchor_diag_bad_kernels.py":23:8): error: failed to legalize operation 'tt.elementwise_inline_asm' that was explicitly marked illegal
```

`summary.json` 中也记录了同样的失败点：

```json
{
  "ok": false,
  "pipeline": "triton-linalg",
  "failed_index": 7,
  "failed_pass": "triton_linalg.triton_to_linalg",
  "mlir_location": {
    "file": "/workspace/triton-anchor/ops-diagnose-cli/test/anchor_diag_bad_kernels.py",
    "line": 23,
    "column": 8,
    "operation": "tt.elementwise_inline_asm"
  }
}
```

结论：该故障样例可以清晰定位到：

```text
pipeline: triton-linalg
pass: 7/14 triton_linalg.triton_to_linalg
source location: /workspace/triton-anchor/ops-diagnose-cli/test/anchor_diag_bad_kernels.py:23:8
MLIR op: tt.elementwise_inline_asm
before IR: /workspace/triton-anchor/ops-diagnose-cli/test/bad-inline-linalg-diagnose/07-triton_linalg.triton_to_linalg.before.mlir
diagnostic detail: /workspace/triton-anchor/ops-diagnose-cli/test/bad-inline-linalg-diagnose/07-triton_linalg.triton_to_linalg.diagnostic.txt
```

这说明当前首版诊断 CLI 对真实 adapter pass 失败可以定位到 pass、源 location、MLIR op，并能保留失败前 IR 快照。

仓库内保留的测试产物：

```text
ops-diagnose-cli/test/anchor_diag_bad_kernels.py
ops-diagnose-cli/test/bad-inline-ttir-diagnose.log
ops-diagnose-cli/test/bad-inline-ttir-diagnose/
ops-diagnose-cli/test/bad-inline-linalg-diagnose.log
ops-diagnose-cli/test/bad-inline-linalg-diagnose/
```

### 5.7 Sophgo PPLIR 失败定位验证

为了验证 TTIR 和 `triton-linalg` 都通过后，工具仍能定位 PPLIR 阶段失败，本次新增 PPLIR 错误样例：

```text
ops-diagnose-cli/test/anchor_diag_bad_pplir_kernel.py
```

故意引入的问题是 2D 到 3D broadcast：

```python
@triton.jit
def bad_pplir_cdim_broadcast_kernel(
    x_ptr,
    out_ptr,
    BLOCK_A: tl.constexpr,
    BLOCK_B: tl.constexpr,
    BLOCK_C: tl.constexpr,
):
    offs_a = tl.arange(0, BLOCK_A)
    offs_b = tl.arange(0, BLOCK_B)
    offs_c = tl.arange(0, BLOCK_C)
    x = tl.load(x_ptr + offs_a[:, None] * BLOCK_C + offs_c[None, :])
    y = x[:, None, :] + tl.zeros((BLOCK_A, BLOCK_B, BLOCK_C), tl.float32)
    out_offsets = (
        offs_a[:, None, None] * BLOCK_B * BLOCK_C
        + offs_b[None, :, None] * BLOCK_C
        + offs_c[None, None, :]
    )
    tl.store(out_ptr + out_offsets, y)
```

这个 kernel 会生成 `linalg.broadcast`：

```text
%broadcasted = linalg.broadcast ins(%collapsed : tensor<4x16xf32>) outs(%6 : tensor<4x8x16xf32>) dimensions = [1]
```

该 broadcast 在 Sophgo `LinalgToPPL` 中触发 C 维 broadcast 不支持分支：

```text
C dimension affected, case not supported yet.
```

验证命令：

```bash
cd /workspace/triton-anchor
source /opt/venv/bin/activate

TRITON_ANCHOR_DIAGNOSE_ON_ERROR=1 \
TRITON_ANCHOR_DIAGNOSE_DIR=/workspace/triton-anchor/ops-diagnose-cli/test/bad-pplir-diagnose-check-4 \
PYTHONDONTWRITEBYTECODE=1 \
/opt/venv/bin/python ops-diagnose-cli/test/anchor_diag_bad_pplir_kernel.py
```

实际结果：

```text
OK: pipeline ttir completed, 7/7 passes executed.
OK: pipeline triton-linalg completed, 14/14 passes executed.
FAILED: pipeline sophgo-pplir failed at pass 2/4: sophgo.linalg_to_ppl
before IR: /workspace/triton-anchor/ops-diagnose-cli/test/bad-pplir-diagnose-check-4/sophgo-pplir/02-sophgo.linalg_to_ppl.before.mlir
diagnostic detail: /workspace/triton-anchor/ops-diagnose-cli/test/bad-pplir-diagnose-check-4/sophgo-pplir/02-sophgo.linalg_to_ppl.diagnostic.txt
location: /workspace/triton-anchor/ops-diagnose-cli/test/anchor_diag_bad_pplir_kernel.py:28:24
operation: linalg.broadcast
ir line: 19: %broadcasted = linalg.broadcast ins(%collapsed : tensor<4x16xf32>) outs(%6 : tensor<4x8x16xf32>) dimensions = [1]  loc(#loc2)
error: PassManager::run failed
summary: /workspace/triton-anchor/ops-diagnose-cli/test/bad-pplir-diagnose-check-4/sophgo-pplir/summary.json
```

诊断详情文件包含底层 PPLIR 报错：

```text
pass: sophgo.linalg_to_ppl
location:
  file: /workspace/triton-anchor/ops-diagnose-cli/test/anchor_diag_bad_pplir_kernel.py
  line: 28
  column: 24
  operation: linalg.broadcast
  ir_line: 19
  ir_snippet: %broadcasted = linalg.broadcast ins(%collapsed : tensor<4x16xf32>) outs(%6 : tensor<4x8x16xf32>) dimensions = [1]  loc(#loc2)
captured diagnostics:
C dimension affected, case not supported yet.
apply triton linalg to ppl conversion failed!!!
```

结论：该样例证明当前诊断可以跨过 TTIR 和 `triton-linalg`，定位到 PPLIR 阶段的首个失败 pass，并进一步定位到具体 `linalg.broadcast` op 和 Python 源码位置。

`ops-diagnose-cli/test/bad-pplir-diagnose-check-4/` 是验证运行产物，不属于功能源码。

### 5.8 console script 注意事项

如果执行：

```bash
triton-anchor-diagnose input.ttir --pipeline ttir --output-dir /workspace/triton-anchor/diagnose-output/ttir
```

出现：

```text
bash: triton-anchor-diagnose: command not found
```

通常说明当前 Python 环境尚未安装包含 console script 的 `triton-anchor` wheel，或者当前 shell 没有激活对应虚拟环境。按仓库构建文档安装后，该命令会生成到虚拟环境的 `bin/` 目录下。

```bash
cd /workspace/triton-anchor
source /opt/venv/bin/activate
source envsetup.sh
uv build --wheel --no-build-isolation
uv pip install dist/triton_anchor-*.whl
```

安装完成后确认：

```bash
which triton-anchor-diagnose
triton-anchor-diagnose --help
```

如果正在开发源码但还未重新安装 wheel，也可以临时使用模块入口验证功能：

```bash
cd /workspace/triton-anchor
PYTHONPATH=/workspace/triton-anchor/python:/workspace/triton-anchor python3 -m triton_anchor.diagnose --help
```

## 6. 使用示例

诊断 TTIR 文件：

```bash
triton-anchor-diagnose input.ttir --pipeline ttir --output-dir /workspace/triton-anchor/diagnose-output/ttir
triton-anchor-diagnose input.ttir --pipeline triton-linalg --output-dir /workspace/triton-anchor/diagnose-output/triton-linalg
```

不预先 dump IR，直接从 Python `@triton.jit` kernel 生成内存 TTIR 并诊断：

```bash
triton-anchor-diagnose \
  --python tests.test_smoke:_smoke_add_kernel \
  --signature '*fp32,*fp32,*fp32,i32' \
  --constant BLOCK=256 \
  --pipeline ttir \
  --output-dir /workspace/triton-anchor/diagnose-output/smoke-python
```

失败输出示例：

```text
FAILED: pipeline ttir failed at pass 4/7: ttir.reorder_broadcast
before IR: /workspace/triton-anchor/diagnose-output/ttir/04-ttir.reorder_broadcast.before.mlir
diagnostic detail: /workspace/triton-anchor/diagnose-output/ttir/04-ttir.reorder_broadcast.diagnostic.txt
location: <stdin>:12:8
operation: tt.load
ir line: 12: %0 = "tt.load"(...)
error: <exception message>
diagnostic output: /workspace/triton-anchor/diagnose-output/ttir
summary: /workspace/triton-anchor/diagnose-output/ttir/summary.json
```

成功输出示例：

```text
OK: pipeline ttir completed, 7/7 passes executed.
diagnostic output: /workspace/triton-anchor/diagnose-output/ttir
summary: /workspace/triton-anchor/diagnose-output/ttir/summary.json
```

Sophgo JIT 编译失败自动诊断：

```bash
cd /workspace/triton-sophgo-backend
TRITON_ANCHOR_DIAGNOSE_ON_ERROR=1 \
TRITON_ANCHOR_DIAGNOSE_DIR=/workspace/triton-anchor/ops-diagnose-cli/test/jit-auto-diagnose \
python3 tests/test_jit.py
```

仓库内故障样例可直接运行触发编译失败和诊断：

```bash
cd /workspace/triton-anchor
source /opt/venv/bin/activate

TRITON_ANCHOR_DIAGNOSE_ON_ERROR=1 \
python /workspace/triton-anchor/ops-diagnose-cli/test/anchor_diag_bad_kernels.py
```

该样例会直接从 `@triton.jit` 函数生成内存 TTIR，然后依次诊断 TTIR pipeline 和 `triton-linalg` pipeline。默认诊断产物写入：

```text
/workspace/triton-anchor/ops-diagnose-cli/test/auto-diagnose/
```

验证 Python/Triton frontend 生成 TTIR 前失败：

```bash
cd /workspace/triton-anchor
source /opt/venv/bin/activate

TRITON_ANCHOR_DIAGNOSE_ON_ERROR=1 \
python /workspace/triton-anchor/ops-diagnose-cli/test/anchor_diag_bad_frontend_kernel.py
```

该样例故意调用不存在的 Triton language builtin：

```text
tl.this_builtin_does_not_exist(x)
```

实际输出：

```text
FAILED: python-frontend failed before TTIR generation.
python target: anchor_diag_bad_frontend_kernel:bad_frontend_kernel
diagnostic detail: /workspace/triton-anchor/ops-diagnose-cli/test/bad-frontend-diagnose/python-frontend.diagnostic.txt
location: 6:8
error: at 6:8:
...
AttributeError("module 'triton.language' has no attribute 'this_builtin_does_not_exist'")
diagnostic output: /workspace/triton-anchor/ops-diagnose-cli/test/bad-frontend-diagnose
summary: /workspace/triton-anchor/ops-diagnose-cli/test/bad-frontend-diagnose/summary.json
```

说明：

- 该方式不只覆盖 `tests/test_jit.py` 里已有的四个算子。
- 任意新增算子只要通过 Sophgo 后端 `fn[grid](...)` 编译，并在 TTIR、triton-linalg、PPLIR、`ppl-compile`、CMake / `.so` 或 runtime launch 阶段失败，都会自动输出 `[AnchorDiagnose]` 诊断信息。
- 如果走 Sophgo JIT 后端自动 hook，Python 文件必须执行 `kernel[grid](...)` 才会触发编译；如果使用 `anchor_diag_bad_kernels.py` 这种脚本内诊断入口，则可以不依赖实际 tensor launch。
- 结果/精度错误需要 expected output；当前 Sophgo 测试 `assert_close()` 已接入 result mismatch 诊断。

