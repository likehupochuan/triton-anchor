# CI docs-only probe

This temporary document is used to verify that a pull request containing only
documentation changes follows the docs-only CI path.

It does not change project behavior and can be removed when the CI verification
is complete.
