diff --git a/.github/PULL_REQUEST_TEMPLATE.md b/.github/PULL_REQUEST_TEMPLATE.md
index 05031bd..08a011a 100644
--- a/.github/PULL_REQUEST_TEMPLATE.md
+++ b/.github/PULL_REQUEST_TEMPLATE.md
@@ -1,9 +1,9 @@
 <!-- 请简要填写以下三项；纯文档变更可在验证部分说明未运行测试及原因。 -->
-## 变更概述 / Summary
+#### 变更概述 / Summary
 <!-- field:summary -->
 
-## 影响范围 / Scope
+#### 影响范围 / Scope
 <!-- field:scope -->
 
-## 验证情况 / Validation
+#### 验证情况 / Validation
 <!-- field:validation -->
diff --git a/.github/workflows/ci-gateway.yml b/.github/workflows/ci-gateway.yml
index 7940c6e..c3dfac1 100644
--- a/.github/workflows/ci-gateway.yml
+++ b/.github/workflows/ci-gateway.yml
@@ -119,7 +119,6 @@ jobs:
             const content = data => JSON.stringify({schema: data?.schema, tasks: data?.tasks, site_digest: data?.site_digest});
             core.setOutput('changed', content(previous) !== content(next));
       - uses: actions/upload-pages-artifact@v3
-        if: steps.dashboard.outputs.changed == 'true'
         with: {path: _site}
       - name: Surface incomplete receiver operations
         if: inputs.mode == 'receive' && steps.collect.outputs.receiver_errors != '0'
@@ -145,7 +144,7 @@ jobs:
   deploy-dashboard:
     name: Deploy dashboard (independent of test results)
     needs: publish
-    if: ${{ !cancelled() && needs.publish.result == 'success' && needs.publish.outputs.dashboard_changed == 'true' }}
+    if: ${{ !cancelled() && needs.publish.result == 'success' }}
     runs-on: ubuntu-latest
     timeout-minutes: 10
     concurrency:
diff --git a/.github/workflows/ci-request.yml b/.github/workflows/ci-request.yml
index 6707cb7..f35a01f 100644
--- a/.github/workflows/ci-request.yml
+++ b/.github/workflows/ci-request.yml
@@ -1,9 +1,11 @@
 name: CI Request
 run-name: >-
-  ${{ github.event_name == 'pull_request_target' && format('PR #{0}', github.event.pull_request.number) || format('Branch {0}', inputs.source_branch) }} | request
+  ${{ github.event_name == 'pull_request_target' && format('PR #{0}', github.event.pull_request.number) || format('Branch {0}', github.ref_name || inputs.source_branch) }} | request
 on:
   pull_request_target:
     types: [opened, synchronize, reopened, ready_for_review, edited, closed, converted_to_draft, labeled, unlabeled]
+  push:
+    branches: ['**']
   workflow_dispatch:
     inputs:
       source_branch: {type: string, required: true, description: 'Branch to validate'}
@@ -11,7 +13,7 @@ on:
 permissions: {}
 concurrency:
   group: >-
-    local-ci-request-${{ github.event_name == 'pull_request_target' && format('pr-{0}', github.event.pull_request.number) || format('branch-{0}', inputs.source_branch) }}
+    local-ci-request-${{ github.event_name == 'pull_request_target' && format('pr-{0}', github.event.pull_request.number) || format('branch-{0}', github.ref_name || inputs.source_branch) }}
   cancel-in-progress: true
 jobs:
   route:
@@ -30,10 +32,11 @@ jobs:
             const {owner, repo} = context.repo;
             const pull = context.payload.pull_request;
             const manual = context.eventName === 'workflow_dispatch';
+            const push = context.eventName === 'push';
             const shaPattern = /^[0-9a-f]{40}$/i;
-            let branch = pull?.base.ref || '';
-            let requestedSha = pull?.head.sha || '';
-            let action = context.payload.action || '';
+            let branch = pull?.base.ref || (push ? context.ref_name : '');
+            let requestedSha = pull?.head.sha || (push ? context.sha : '');
+            let action = context.payload.action || (push ? 'push' : '');
             if (manual) {
               const {data: repository} = await github.rest.repos.get({owner, repo});
               const runRef = context.ref.replace('refs/heads/', '');
@@ -56,7 +59,7 @@ jobs:
               }
               action = 'manual';
             }
-            if (!pull && !manual) throw new Error(`Unsupported event ${context.eventName}`);
+            if (!pull && !manual && !push) throw new Error(`Unsupported event ${context.eventName}`);
             if (!branch || !shaPattern.test(requestedSha)) throw new Error('Invalid branch routing request');
             // When the full Gateway moves to main, this is the only migration switch.
             const controlRef = 'local-ci-unified';
